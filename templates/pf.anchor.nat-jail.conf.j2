{{ ansible_managed | comment }}

# nat http(s) requests to update Joomla
nat pass log on {{ host_net_ext_if }} proto tcp \
  from {{ jail_net_ip }} \
  to any port { http,https } \
  -> {{ host_net_ext_ip }}
