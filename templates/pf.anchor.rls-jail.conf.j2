{{ ansible_managed | comment }}

# allow traffic to DB
pass log on {{ host_net_int_if }} proto tcp \
  from {{ jail_net_ip }} \
  to {{ joomla_db_host }} port 3306 \
  label {{ joomla_server_name }}-mysql
